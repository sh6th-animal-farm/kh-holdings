<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kanghwang.khholdings.domain.my.MyRepository">

    <select id="selectWalletById" resultType="WalletDTO">
        WITH latest_token_quotes AS (
            SELECT DISTINCT ON (token_id)
                token_id,
                closing_price
            FROM token_quotes
            WHERE unit = 1
            ORDER BY token_id, candle_time DESC
        ),
        wallet_stats AS (
            SELECT
                w.wallet_id,
                w.bank_name,
                w.account_no,
                w.cash_balance,
                COALESCE(SUM(h.purchased_value), 0) AS total_purchased_value,
                COALESCE(SUM(h.token_balance * ltq.closing_price), 0) AS total_market_value,
                w.frozen_amount
            FROM wallets w
                LEFT JOIN holdings h ON w.wallet_id = h.wallet_id
                LEFT JOIN latest_token_quotes ltq ON h.token_id = ltq.token_id
            WHERE w.wallet_id = #{walletId}
            GROUP BY w.wallet_id, w.bank_name, w.account_no, w.cash_balance
        )
        SELECT
            bank_name,
            account_no,
            cash_balance,
            total_purchased_value,
            total_market_value,
            (cash_balance + total_market_value) AS totalBalance,
            (total_market_value - total_purchased_value) AS profitLoss,
            frozen_amount,
            CASE
                WHEN total_purchased_value = 0 THEN 0
                ELSE (total_market_value - total_purchased_value) / total_purchased_value * 100
                END AS profitLossRate
        FROM wallet_stats
    </select>

    <select id="selectTokenByWalletId" parameterType="map" resultType="HoldingDTO">
        WITH latest_token_quotes AS (
            SELECT DISTINCT ON (token_id)
            token_id,
            closing_price
        FROM token_quotes
        WHERE unit = 1
        ORDER BY token_id, candle_time DESC
            )
        SELECT
            t.token_name           AS tokenName,
            t.ticker_symbol        AS tickerSymbol,
            h.token_balance        AS tokenBalance,
            h.purchased_value      AS purchasedValue,
            (h.token_balance * ltq.closing_price) AS marketValue,
            (h.token_balance * ltq.closing_price) - h.purchased_value AS profitLoss,
            CASE
                WHEN h.purchased_value = 0 THEN 0
                ELSE
                    ((h.token_balance * ltq.closing_price) - h.purchased_value)
                        / h.purchased_value * 100
                END AS profitLossRate
        FROM holdings h
                 JOIN tokens t
                      ON h.token_id = t.token_id
                 LEFT JOIN latest_token_quotes ltq
                           ON h.token_id = ltq.token_id
        WHERE h.wallet_id = #{walletId}
        ORDER BY t.token_name
            LIMIT 10
        OFFSET (#{page} - 1) * 10
    </select>

    <select id="selectTxHistByWalletId" resultType="TransactionHistDTO">
        SELECT
            MIN(th.created_at) AS created_at,
            CASE
                WHEN th.transaction_type = 'TRADE' AND MAX(o.order_side) = 'BUY' THEN 'BUY'
                WHEN th.transaction_type = 'TRADE' AND MAX(o.order_side) = 'SELL' THEN 'SELL'
                ELSE th.transaction_type::text
                END AS transaction_type,
            COALESCE(MAX(t.token_name), '-') AS token_name,
            COALESCE(MAX(t.token_id), 0) AS token_id,
            CASE
                WHEN th.transaction_type = 'TRADE' THEN
                    ABS(SUM(CASE WHEN th.asset_type = 'CASH' THEN th.amount ELSE 0 END)) /
                    NULLIF(SUM(CASE WHEN th.asset_type = 'TOKEN' THEN th.amount ELSE 0 END), 0)
                ELSE NULL
                END AS unit_price,
            SUM(CASE WHEN th.asset_type = 'TOKEN' THEN th.amount ELSE 0 END) AS transaction_volume,
            MAX(CASE WHEN th.asset_type = 'TOKEN' THEN th.balance_after END) AS token_balance_after,
            SUM(CASE WHEN th.asset_type = 'CASH' THEN th.amount ELSE 0 END) AS transaction_amount,
            MAX(CASE WHEN th.asset_type = 'CASH' THEN th.balance_after ELSE NULL END) AS balance_after,
            COALESCE(th.trade_id, th.transaction_id) AS group_id
        FROM transaction_hists th
                 LEFT JOIN orders o ON th.order_id = o.order_id
                 LEFT JOIN tokens t ON o.token_id = t.token_id
        WHERE th.wallet_id = #{walletId}
        <if test="category != null and category != ''">
            AND th.transaction_type = #{category}
        </if>
        <if test="period != null and period != ''">
            <choose>
                <when test="period == '1W'">
                    AND created_at >= NOW() - INTERVAL '7 days'
                </when>
                <when test="period == '1M'">
                    AND created_at >= NOW() - INTERVAL '1 month'
                </when>
                <when test="period == '3M'">
                    AND created_at >= NOW() - INTERVAL '3 months'
                </when>
                <when test="period == '6M'">
                    AND created_at >= NOW() - INTERVAL '6 months'
                </when>
            </choose>
        </if>
        GROUP BY
            COALESCE(th.trade_id, th.transaction_id),
            th.transaction_type
        ORDER BY MIN(th.created_at) DESC
            LIMIT 10
        OFFSET <![CDATA[ (#{page} - 1) * 10 ]]>
    </select>

    <select id="selectAccount">
        SELECT
            wallet_id
        FROM
            wallets
        WHERE user_id = #{userId}
        AND bank_name = 'kh 증권'
    </select>

</mapper>
